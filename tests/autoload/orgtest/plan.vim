function! orgtest#plan#at() abort " {{{1
  call assert_report("No test for org#plan#at(lnum)")
  " call assert_equal(, org#plan#at(lnum))
endfunction

function! orgtest#plan#insert() abort " {{{1
  call assert_report("No test for org#plan#set(lnum, timestamp, ...)")
  " call assert_equal(, org#plan#set(lnum, timestamp, ...))
endfunction

function! orgtest#plan#islate() abort " {{{1
  call assert_report("No test for org#plan#islate(ts, ...)")
  " call assert_equal(, org#plan#islate(ts, ...))
endfunction

function! orgtest#plan#isplanned() abort " {{{1
  call assert_report("No test for org#plan#isplanned(ts, ...)")
  " call assert_equal(, org#plan#isplanned(ts, ...))
endfunction

function! orgtest#plan#issoon() abort " {{{1
  call assert_report("No test for org#plan#issoon(ts, ...)")
  " call assert_equal(, org#plan#issoon(ts, ...))
endfunction

function! orgtest#plan#nearest_plan() abort " {{{1
  call assert_report("No test for org#plan#nearest(ts, ...)")
  " call assert_equal(, org#plan#nearest(ts, ...))
endfunction

function! orgtest#plan#remove() abort " {{{1
  call assert_report("No test for org#plan#remove(lnum)")
  " call assert_equal(, org#plan#remove(lnum))
endfunction

function! orgtest#plan#set() abort " {{{1
  call assert_report("No test for org#plan#set(lnum, timestamp, ...)")
  " call assert_equal(, org#plan#set(lnum, timestamp, ...))
endfunction

